#ifndef ONLINE_JUDGE
freopen("int.txt","r",stdin);
chrono::high_resolution_clock::time_point start_t, end_t;
start_t = chrono::high_resolution_clock::now();
#endif
int t = scan();/*scanf("%d",&t);*/ while (t--) {

}
#ifndef ONLINE_JUDGE
end_t = chrono::high_resolution_clock::now();
auto duration = chrono::duration_cast<chrono::milliseconds>(end_t - start_t);
cerr<<"\nRunTime: "<<duration.

count()

<<"ms"<<'\n';
#endif
